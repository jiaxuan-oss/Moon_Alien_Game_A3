package game;

/**
 * Use this enum to represent abilities.
 * Example #1: if the player is capable jumping over walls, you can attach Ability.WALL_JUMP to the player class
 * Modified by: Hoe Jing Yang
 */
public enum Ability {
    /**
     * provides the actor the capability to enter the spaceship
     */
    ABILITY_ENTER_SPACESHIP,
    /**
     * provides the actor the capability to trade with the player
     */
    ABILITY_TRADING
}
